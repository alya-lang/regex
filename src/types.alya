# Types and data structures for regex
# Part of the Alya Language package ecosystem (https://github.com/alya-lang)

struct RegexConfig
    name
    prefix
    count
end

function make_config(name = "World", count = 1)
    return RegexConfig {
        name: name,
        prefix: "Hello",
        count: count
    }
end
