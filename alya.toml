[package]
name = "regex"
version = "0.1.0"
alya-version = "0.0.17"
entry = "src/lib.alya"
description = "High-performance regular expression engine with linear-time matching, named capture groups, lookarounds, backreferences, and Unicode property classes"
authors = ["Alya Language Contributors <https://github.com/alya-lang>"]
license = "MIT"
homepage = "https://github.com/alya-lang/alya"
repository = "https://github.com/alya-lang/regex"
keywords = ["alya", "alya-lang", "package", "regex", "regexp", "pattern", "matcher"]

[dependencies]

# [build]
# c-sources = ["c/source.c"]
# c-flags = ["-O2"]
# c-include-dirs = ["c"]
